database:
    cmd:
      docker compose up mssql_server

server:
    var:
      basic:
        username: harry
        address: Adelaide
        num_floors: 2
        house_type: Townhouse
      option:
        all: "-a"
      sequence:
        rooms
    env:
      ADDR: $(address)
    cmd:
      - sleep 10 && echo "Wake Up after 10s" &
      - sleep 5 && echo "Wake Up after 5s" &
