commit:
    var:
        basic:
            message: null
        option:
            interactive: "-i"
            verbose: "-v"
            all: "-A"
        sequence:
            pathspec: .
    cmd:
      - git add $(verbose) $(interactive) $(all) $(pathspec)
      - git commit $(verbose) --message "$(message)"